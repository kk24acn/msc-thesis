# Hardhat Node

Local Ethereum blockchain simulator for transaction verification.

## Execution
It is recommended to start the Hardhat node using the root `docker-compose.yml`.

To start the Hardhat container using Docker Compose from the project root, run:

```sh
docker compose up -d hardhat
```