# UI Dashboard

A React UI Dashboard for monitoring transaction batch execution in real-time.

## Execution

It is recommended to run the Dashboard via the root `docker-compose.yml` to ensure that all containers it depends on are running and linked together.
